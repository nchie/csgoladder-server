var bookshelf = require('../config').bookshelf;

var TeamInvite = bookshelf.Model.extend({
	idAttribute: ['user_id', 'team_id'],
	tableName: 'team_invite'
});

module.exports = bookshelf.model('TeamInvite', TeamInvite);